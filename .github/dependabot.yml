version: 2
updates:
  # This project has no third-party dependencies: scan.py imports only the standard
  # library. The workflow's actions are the one thing that goes stale on its own.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 2
    commit-message:
      prefix: ci
